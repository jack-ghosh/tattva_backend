import { db } from "../lib/db.ts";
import { questions } from "../lib/schema.ts";
import { users } from "../lib/schema.ts";

async function seed() {
    // await db.insert(users).values([
    // ])
}

seed()